# chanstore 플랫폼 통합 기획안 — 데이터수집 · AI 상세페이지 · AI CS

> 이 문서는 **전체 플랫폼**의 노선·제약·로드맵을 고정하는 상위 기준 문서입니다.
> 1단계 수집기의 세부 노선/제약은 [`PROJECT.md`](./PROJECT.md)를 함께 따릅니다.
> 두 문서가 충돌하면 **더 보수적인 쪽(안 깨지고, 계정정지 안 나는 쪽)**을 택합니다.

---

## 0. 한 줄 정의

국내 위탁판매(도매처 → 국내 오픈마켓) 운영을 위해
**① 경쟁 시장 데이터를 안전하게 수집하고 → ② AI로 상세페이지를 반자동 생성하고 → ③ AI로 CS를 반자동 처리**하는
데스크톱/서버형 통합 도구.

세 기능은 **서로 독립 모듈**이다. 하나(예: 쿠팡 크롤러)가 막혀도 나머지는 계속 돈다.

---

## 1. 먼저 바로잡을 3가지 (기획 전제)

요청 원문 중 사실관계상 조정이 필요한 부분을 먼저 고정한다.

### 1-1. "원가"는 크롤링으로 못 얻는다
오픈마켓에 **공개되는 건 판매가(최저가/정상가/할인가)뿐**이다. 매입원가(도매단가)는 판매자만 아는 비공개 정보라
크롤링·API 어디에도 없다.

- 크롤링으로 얻는 것 → **판매가, 최저가, 배송비, 쿠폰가** (경쟁 가격 인텔리전스)
- 원가 → **소싱처(도매매·온채널·도매꾹·오너클랜 등)에서** 별도로 입력/연동
- 따라서 "원가"는 **크롤링 필드가 아니라, 소싱처 데이터로 채워 넣는 별도 입력값**으로 설계한다.
- 마진 = (내 판매가 예정) − (소싱 원가 + 마켓수수료 + 배송비 + 부가세) → 이건 **분석 레이어**가 계산.

### 1-2. 쿠팡은 "공개 검색 API 없음 + 봇탐지 최상급"
쿠팡에는 외부인이 쓸 **공개 상품검색 API가 없다.** (있는 건 ① 쿠팡파트너스 제휴 API — 링크/커미션용, ② WING 판매자 API — 내 상품 관리용. 둘 다 "남의 상품 시세 조사"엔 못 씀.)
크롤링은 Akamai 급 봇탐지 + 동적 렌더링 + 로그인 유도로 **막힐 확률과 리스크가 가장 크다.**
→ **쿠팡은 우선순위 최하위, 선택적 모듈**로 둔다. 무리하게 뚫지 않는다.

### 1-3. 스마트스토어 "타 셀러"는 직접 API가 안 된다
- 네이버 커머스 API(스마트스토어)는 **내 스토어**만 다룬다(주문·상품·CS).
- **경쟁 스토어 시세 조사**는 → **네이버 쇼핑 검색 오픈API**로 우회한다(스토어명·최저가·몰 정보가 붙어 나옴).
- 즉 "스마트스토어 수집" 요구는 실제로는 **네이버 쇼핑 검색 API 수집**으로 구현된다.

---

## 2. 데이터 소스별 전략표 (핵심)

우선순위 원칙: **공식 API > 반공식 > 크롤링(최후)**. 안 깨지는 파이프라인이 목표.

| 마켓 | 공개 수집 경로 | 방식 | 봇탐지/리스크 | 우선순위 | 비고 |
|------|---------------|------|--------------|---------|------|
| **11번가** | 오픈API `ProductSearch` | **API** | 낮음 | ★1 | 구현완료. 응답 XML(EUC-KR) |
| **네이버(+스마트스토어 우회)** | 쇼핑 검색 오픈API | **API** | 낮음 | ★2 | client id/secret. 하루 25,000콜 제한 |
| **지마켓** | 없음(약함) | 크롤링 | 중~높음 | 3 | ESM 계열, 동적요소 |
| **옥션** | 없음(약함) | 크롤링 | 중~높음 | 3 | 지마켓과 동일 ESM |
| **쿠팡** | 없음(공개검색 API 부재) | 크롤링(선택) | **최상** | 4(선택) | 무리 금지. 막히면 스킵 |

> 원칙: **API로 되는 마켓(11번가·네이버)에서 최대한 뽑고, 크롤링은 "그 마켓에만 있는 상품"을 볼 때만** 최소로 켠다.

---

## 3. 크롤링 주의사항 — "봇 안 걸리게" 설계 규칙

요청하신 "사이트마다 주의사항"을 코드 규칙으로 못박는다. **목적은 회피가 아니라 '민폐 안 끼치고 안정적으로, 계정/IP 안 날리기'.**

### 3-1. 기술 규칙 (안정성 · 매너 크롤링)
- **robots.txt 존중**: 수집 전 해당 경로 허용 여부 확인. 명시적으로 막힌 곳은 안 긁는다.
- **호출 간 랜덤 딜레이**: 고정 0.5초 X → 1.5~4초 사이 지터(jitter). 사람처럼 불규칙하게.
- **동시성 제한**: 마켓당 동시 요청 1~2개. 폭주 금지. (rate limiter 공용 모듈)
- **정직한 요청 헤더**: 정상 브라우저 UA/Accept-Language. UA를 매 요청 무작위로 바꾸는 건 오히려 봇 시그널 → 세션 단위로 고정.
- **세션·쿠키 유지**: 요청마다 새 세션 X. 브라우저처럼 세션 재사용.
- **캐싱 필수**: 같은 상품 24h 내 재요청 금지. 증분 수집(변한 것만). → 트래픽·탐지 동시에 절감.
- **실패 시 지수 백오프 + 서킷 브레이커**: 403/429/캡차가 뜨면 **즉시 그 마켓 수집 중단**하고 쿨다운. 밀어붙이지 않는다(밀면 밴).
- **영업시간 외 분산**: 새벽 몰빵 X, 하루에 얇게 펴서.
- **헤드리스 티 지우기(지마켓/옥션·쿠팡)**: 필요 시 Playwright + stealth. 단 이건 "탐지 회피 군비경쟁"이라 **최후수단**이고, 여기 의존하는 순간 파이프라인이 취약해짐을 전제로.
- **IP 로테이션/프록시**: **회색지대.** 기본은 단일 IP + 저속. 프록시는 (a) 정말 필요하고 (b) 합법 프록시일 때만, 소스별 플래그로 옵트인. 남의 마켓을 대규모로 두들기는 용도로는 쓰지 않는다.

### 3-2. 법적·정책 주의사항 (계정정지·소송 리스크)
- **약관(ToS)**: 대부분 마켓 약관이 자동수집을 제한한다. → 그래서 **사실 데이터·저빈도·비상업적 분석용**으로 스스로 한계를 긋는다(PROJECT.md의 "B 노선").
- **저작권(이미지·상세설명)**: 경쟁사 **이미지/상세 HTML을 저장→가공→내 판매에 재사용 금지.** URL은 분석 참고용 표시까지만(§5 이미지 정책과 연결).
- **DB권·부정경쟁방지법**: 남의 DB를 통째로 복제해 서비스화하면 위법 소지. → **가공·분석된 지표만 산출**, 원본 대량복제 배포 금지.
- **개인정보**: 리뷰어 닉네임/구매자 정보 등은 수집·저장하지 않는다. 집계 수치(리뷰 수/평점)만.
- **판매자 계정 분리**: 크롤링에 쓰는 IP/환경과 **내 판매자 계정 로그인 환경을 분리**한다(연좌 밴 방지).

---

## 4. 3대 기능 아키텍처

```
                    ┌─────────────────────────────────────────┐
                    │            공통 코어 (core)               │
                    │  schema(Product/Listing) · config · db   │
                    │  rate-limiter · cache · logging · queue   │
                    └─────────────────────────────────────────┘
                        ▲              ▲                ▲
        ┌───────────────┘   ┌──────────┘    ┌──────────┘
┌───────┴────────┐  ┌───────┴────────┐  ┌───┴──────────────┐
│ ① 수집          │  │ ② AI 상세페이지 │  │ ③ AI CS          │
│  sources/*      │  │  content/*      │  │  cs/*            │
│  11st(API)      │  │  카피 생성(LLM)  │  │  문의 수집        │
│  naver(API)     │  │  이미지 생성     │  │  분류/의도파악    │
│  gmarket(크롤)  │  │  (nano banana)  │  │  답변초안 생성    │
│  auction(크롤)  │  │  템플릿 조립     │  │  사람 검수 후 발송 │
│  coupang(선택)  │  │  마켓 규격 변환  │  │  (human-in-loop) │
└─────────────────┘  └─────────────────┘  └──────────────────┘
```

- 이미 있는 `BaseSource.search()` 인터페이스를 **그대로 확장**. 새 마켓/기능은 인터페이스만 맞추면 파이프라인에 꽂힌다.
- 모듈 간 통신은 **공통 스키마 + DB/큐**로만. 직접 호출 의존성 최소화.

---

## 5. 기능 ① 데이터 수집 (확장)

### 수집 필드 (현 스키마 + 추가)
현 `Product`(source, product_code, name, **price(판매가)**, image_url, product_url, seller, review_count, rating, category, rank, delivery, collected_at)에 아래를 확장:

- `list_price` / `sale_price` / `coupon_price` — 정상가·할인가·쿠폰가 분리
- `shipping_fee` — 배송비
- `sourcing_cost`(원가) — **크롤링 값 아님**. 소싱처 연동/수동 입력으로 채우는 필드(§1-1)
- `estimated_margin` — 분석 레이어 계산값

### 이미지 정책 (요청 "이미지 저장"에 대한 답 — 갱신)
- **경쟁사 메인 썸네일**: URL뿐 아니라 **파일로 내려받아 저장한다**(내부 분석·비교·모니터링용).
  저장 위치는 참고용 저장소 `ref_images/` (소스별 폴더, `image_path` 필드로 추적).
  다운로드도 CDN에 대한 요청이므로 RateLimiter로 예의를 지키고, 이미 받은 건 재다운로드하지 않는다.
- **참고용 ≠ 판매용**: 내려받은 썸네일을 **내 상세페이지에 재가공·재사용**하는 것은 별개의(저작권) 판단이다.
  코드는 저장까지만 하고 재사용을 자동으로 하지 않으며, 참고용/판매용 저장소를 **물리적으로 분리**한다.
- **내 판매용 이미지**: (a) 소싱처가 약관상 허용한 제공 이미지, 또는 (b) 기능 ②가 **AI로 새로 생성**한 이미지
  (`images/`). 실판매 자산은 이 두 경로에서 나온다.

---

## 6. 기능 ② AI 상세페이지 (nano banana + MCP 자동화)

### 개념 정리
- **nano banana = Google Gemini 2.5 Flash Image** (이미지 생성/편집 모델의 별칭). "완전 무료 무제한"은 아니고 **무료 티어(할당량) + 초과 시 종량제**. 기획은 무료 할당량 내 운용 + 필요 시 유료 승격 전제.
- 상세페이지 = **텍스트(카피) + 이미지(연출컷/인포그래픽)** 두 축. 텍스트는 LLM(Claude/Gemini), 이미지는 nano banana.

### 자동화 파이프라인
```
소싱 상품 원본(제목·스펙·제공이미지)
   → [LLM] 판매 카피 생성(제목/셀링포인트/스펙표/주의문구)   ← 사실 왜곡·과장광고 필터
   → [nano banana] 배경 제거·연출컷·상세 인포그래픽 이미지 생성/편집
   → [템플릿 엔진] 마켓별 상세 HTML/이미지 규격으로 조립
   → 사람 최종 검수 → 마켓 업로드(수동 or 판매자 API)
```

### MCP 자동화
- **MCP 서버**로 각 능력을 도구화: `generate_copy`, `generate_image(nano banana)`, `build_detail_page`, `export_for_market`.
- Claude/에이전트가 이 MCP 도구들을 호출해 "상품 하나 넣으면 상세페이지 초안 나옴"을 자동화.
- 이미지 생성 API(Gemini) 호출은 MCP 서버 내부에 캡슐화 → 모델 교체(다른 무료/유료 이미지 모델)해도 상위 흐름 불변.

### 상세페이지 주의사항
- **과장·허위광고 금지**(표시광고법): "최고/1위/100% 효과" 등 근거 없는 표현 자동 차단 규칙. 건강·식품·화장품은 문구 규제 강함.
- **AI 생성 이미지 고지/책임**: 실제 상품과 다른 연출(색·크기 왜곡)은 반품·분쟁 사유 → "이미지는 연출컷" 고지, 실측/실물 정보는 정확히.
- **경쟁사 상세 복제 금지**: §3-2 저작권과 동일. AI는 **새로 창작**, 베끼기 아님.
- **마켓별 규격 차이**: 이미지 사이즈·용량·허용 태그가 마켓마다 다름 → 마켓별 export 어댑터로 흡수.

---

## 7. 기능 ③ AI CS 관리

### 흐름
```
마켓 문의/리뷰/톡톡 수집 → [LLM] 의도분류(배송/교환/환불/상품문의/클레임)
   → 지식베이스(내 상품·정책·FAQ) 기반 답변 초안 생성
   → 긴급/클레임/개인정보성은 '사람 필수' 플래그
   → 운영자 검수·수정 → 발송
```

### 채널
- **스마트스토어**: 네이버 커머스 API(문의·톡톡·리뷰) — 내 스토어라 합법적으로 붙는다.
- **11번가/지마켓/옥션/쿠팡**: 각 셀러 오피스의 CS API/알림 범위 확인 후 연동(마켓별 상이). 없으면 **초안 생성까지만** 하고 발송은 수동.

### CS 주의사항 (여기가 사고 잘 남)
- **Human-in-the-loop 기본값**: AI 자동발송 X. **초안 → 사람 확인 → 발송**. 오답 CS는 클레임·별점테러로 직결.
- **환불/교환/보상 약속 금지 가드레일**: 금액·정책 확정 답변은 사람만. AI는 "확인 후 안내" 톤.
- **개인정보 최소취급**: 주문자 정보는 답변에 필요한 만큼만, 로그에 원문 저장 지양(마스킹).
- **말투·정책 일관성**: 내 스토어 톤·환불정책을 시스템 프롬프트/지식베이스로 고정.
- **거짓 정보(할루시네이션) 차단**: 재고·배송일은 **실제 주문 데이터에서만** 인용, LLM 추측 금지.

---

## 8. 기술 스택

- **언어/코어**: Python 3.x (기존 유지). `httpx`, `xml.etree`, `openpyxl`, `sqlite3`, `python-dotenv`.
- **크롤링(3~5단계)**: `playwright`(+stealth), 공용 rate-limiter/cache 모듈.
- **AI 텍스트**: Claude / Gemini API (LLM 카피·CS 분류·답변초안).
- **AI 이미지**: Gemini 2.5 Flash Image(=nano banana) API.
- **자동화**: MCP 서버(이미지·카피·상세조립 도구화).
- **저장**: SQLite(시작) → 규모 커지면 PostgreSQL. 참고이미지캐시/판매용이미지 저장소 물리 분리.
- **스케줄/큐**: 경량은 APScheduler/파일큐, 확장 시 Redis+RQ/Celery.
- **UI(후순위)**: CLI(현재) → 로컬 대시보드(FastAPI + 간단 프론트).

---

## 9. 단계별 로드맵

- [x] **1단계** — 11번가 공식 API 수집기 *(완료)*
- [ ] **2단계** — 네이버 쇼핑 검색 API 수집(스마트스토어 시세 포함) + 공통 스키마 통합
- [ ] **3단계** — 분석 레이어: 가격분포·마진역산(원가는 소싱처 입력)·핫딜·경쟁강도
- [ ] **4단계** — 지마켓·옥션 크롤러(매너 크롤링 규칙 §3 적용, 서킷브레이커)
- [ ] **5단계** — AI 상세페이지 MCP(카피 LLM + nano banana 이미지 + 마켓별 export)
- [ ] **6단계** — AI CS(스마트스토어 우선, human-in-the-loop)
- [ ] **7단계(선택)** — 쿠팡 크롤러(리스크 최상, 막히면 스킵) / 로컬 대시보드

> 순서 근거: **안 깨지고 합법 리스크 낮은 것(API)부터**, 크롤링·쿠팡은 뒤로. 매출 기여 큰 상세페이지 자동화(5단계)를 CS보다 먼저.

---

## 10. 리스크 요약 & 대응 (요청 "조심할 것들" 총정리)

| 리스크 | 어디서 | 대응 |
|--------|--------|------|
| 봇 차단·IP밴 | 지마켓/옥션/쿠팡 크롤링 | 저속·랜덤딜레이·서킷브레이커·API 우선, 쿠팡 후순위 |
| 판매자 계정 정지 | 크롤링·자동화 전반 | 사실데이터·저빈도, 계정 환경 분리, ToS 자가한계 |
| 저작권 침해 | 이미지/상세 복제 | 경쟁사 이미지=참고용만, 판매용=AI생성/소싱처 |
| 과장·허위광고 | AI 상세페이지 | 금지표현 필터, 실물정보 정확, 연출컷 고지 |
| CS 오답·잘못된 약속 | AI CS 자동발송 | human-in-the-loop, 정책확답은 사람만 |
| 할루시네이션 | 카피/CS | 재고·배송·스펙은 실데이터에서만 인용 |
| 개인정보 | 리뷰어·구매자 | 미수집/마스킹, 집계수치만 |
| 파이프라인 붕괴 | 특정 소스 다운 | 소스별 독립 모듈, 하나 죽어도 나머지 가동 |
| AI 비용 폭증 | 이미지·LLM 종량제 | 무료할당량 내 운용·캐싱·배치, 초과 알람 |

---

## 부록 — 헷갈리면 돌아올 기준
1. **API로 되면 크롤링 안 한다.** 안 깨지는 파이프라인이 크롤링 커버리지보다 중요.
2. **크롤링 이미지·상세는 참고용, 판매 자산은 AI가 새로 만든다.**
3. **AI는 초안까지, 결정·발송은 사람이.** (특히 CS·광고문구)
4. **원가는 크롤링 값이 아니라 소싱처 입력값이다.**
