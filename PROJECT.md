# 위탁판매 시장조사·소싱판단 도구 — 개발 기획안

> 이 문서는 프로젝트의 노선/제약/로드맵을 고정하는 단일 기준 문서입니다.
> 헷갈리면 이 문서로 돌아옵니다.

---

## 0. 한 줄 정의
국내 오픈마켓(11번가·네이버·지마켓·옥션)의 **공개 상품 데이터(가격·랭킹·리뷰수 등 사실 정보)를 수집·분석**하여,
내가 운영하는 **국내 위탁판매(도매처→국내 마켓)의 소싱·가격 판단을 돕는** 데스크톱/스크립트형 도구.

---

## 1. 목적과 비(非)목적 — 노선 고정
이 프로젝트는 아래 **B**에 해당한다. A·C와 혼동하지 말 것.

- **(B) 채택** — 경쟁 마켓을 **분석**해서 내 소싱·가격 판단에 활용 (시장조사 인텔리전스)
- (A) 제외 — 타 마켓 상품을 긁어 그대로 되파는 행위 (아비트라지/복제 셀러). 위탁판매 아님, 계정정지 리스크 큼.
- (C) 제외 — 해외 상품 구매대행. 통관·환율·정품소명 별개 게임.

→ 이 도구의 산출물은 "팔 상품을 베끼는 것"이 아니라 **"무엇을 어디서 얼마에 소싱하면 마진이 남는지 판단하는 근거"**다.

---

## 2. 절대 제약 (반드시 코드/설계에 반영)

### 2-1. 합법성 선 (지식재산권)
- **수집해도 되는 것**: 가격, 상품명, 랭킹/노출순위, 리뷰 수, 판매지표, 카테고리 → 사실(fact) 데이터.
- **하면 안 되는 것**: 경쟁사 **상품 이미지를 저장해 내 상세페이지에 재사용**.
- **이미지 취급 규칙**: 수집한 이미지 URL은 **분석 화면에서 참고용 표시까지만**. DB에 URL은 저장 가능하나, 다운로드→재가공→판매용 재사용 금지.
- **실제 판매용 이미지**: 정식 소싱처(도매매·온채널 등 약관 범위)에서 확보. 이 도구의 범위 밖.

### 2-2. AI 가공 범위
- 텍스트(상품명·상세 문구) 재작성 = 유효.
- 이미지 AI 재생성 = 판매용 사용 금지.

### 2-3. 아키텍처 원칙 — "안 깨지는 것 우선"
- 공식 API가 있는 소스는 **무조건 API로**.
- 크롤링은 공식 경로가 없을 때만, 최후순위로.
- 크롤링 모듈이 깨져도 **API 기반 파이프라인은 계속 동작**하도록 모듈 분리(소스별 독립).

---

## 3. 데이터 소스 분류

| 마켓 | 공개 검색 API | 수집 방식 | 우선순위 | 비고 |
|------|--------------|-----------|---------|------|
| **11번가** | 있음 (오픈API `ProductSearch`) | **API** | 1순위 | 키 발급 필요, 응답 XML(EUC-KR/CP949) |
| **네이버** | 있음 (검색/쇼핑 오픈API) | **API** | 2순위 | client id/secret 발급 필요 |
| **지마켓** | 약함/불명확 | 크롤링 | 후순위 | 봇탐지 강함 |
| **옥션** | 약함/불명확 | 크롤링 | 후순위 | 지마켓과 동일 ESM 계열 |

---

## 4. 단계별 로드맵
- **1단계 — 11번가 공식 API 수집기** ★ (이 저장소의 현재 구현 범위)
- 2단계 — 네이버 쇼핑 API 추가 (공통 스키마로 통합)
- 3단계 — 분석 레이어 (가격분포·마진 역산·핫딜 감지·경쟁강도)
- 4단계 — AI 가공 모듈 (텍스트 한정)
- 5단계 — 지마켓·옥션 크롤링 (최후순위)

---

## 5. 1단계 상세 스펙 (구현 완료 범위)
- 키워드 입력 → 11번가 공개 API로 상품 목록 수집 → xlsx + SQLite 저장
- 추출: 상품명, 판매가, 이미지URL, 상품URL, 판매자명, 리뷰수/평점, 카테고리
- 페이지네이션, 호출 딜레이, 재시도, 에러 핸들링, 중복 처리
- 소스별 모듈 분리 + 공통 데이터 스키마(dataclass)
- API 키는 `.env` 분리

### 완료 조건 (DoD)
- 키워드 "햇반" 실행 시 실제 상품 데이터가 엑셀로 떨어지고 SQLite에 적재
- 2페이지 이상 페이지네이션 동작
- API 키 없이 실행하면 명확한 안내 메시지 출력

---

## 6. 기술 스택
- Python 3.x / `httpx` / `xml.etree` / `openpyxl` / 표준 `sqlite3` / `python-dotenv`
- (5단계 `playwright`, 4단계 LLM API는 추후)

---

## 부록 — 헷갈리면 돌아올 기준
- 가장 중요한 건 크롤링 자체가 아니라 **안 깨지는 데이터 파이프라인** = 크롤링 최소화 + API 최대화.
